<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Albert+Sans:wght@200;400;700&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <slot />
    </body>
</html>

<style>
    :root {
        --primario: #15c0e9;
        --gris: #7a7a7a;
    }
    .container {
        padding: 10% 20% 10% 20%;
        margin-inline: auto;
        font-family: "Albert Sans", sans-serif;
        line-height: 1.5;
        color: var(--gris);
    }

    h1 {
        color: var(--primario);
        font-weight: 700;
        font-size: 40px;
        text-align: center;
    }

    p {
        color: var(--gris);
        font-family: "Albert Sans", Sans-serif;
        font-size: 20;
        font-weight: 200;
        line-height: 20px;
        text-align: left;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    a {
        color: var(--primario);
        font-weight: 500;
        text-decoration: none;
    }

    @media (max-width: 480px) {
    }
</style>
